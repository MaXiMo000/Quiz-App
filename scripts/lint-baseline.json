{
  "_comment": [
    "Recorded lint debt, so the number can only go down.",
    "Most of the backend count is auto-fixable (`npx eslint . --fix`); it is",
    "left for its own change rather than mixed into unrelated work.",
    "Fix some, then lower these — the ratchet fails if they grow, and also if",
    "they shrink without this file being updated."
  ],
  "backend": { "errors": 334, "warnings": 0 },
  "frontend": { "errors": 29, "warnings": 29 }
}
